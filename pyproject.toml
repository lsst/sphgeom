[build-system]
build-backend = "setuptools.build_meta"
requires = [
    "setuptools >= 46.6.0",
    "wheel",
    "pybind11 >= 2.5.0",
    "numpy >= 1.18",
    "lsst-versions",
]

[tool.lsst_versions]
write_to = "python/lsst/sphgeom/version.py"
